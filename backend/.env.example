# ============================================
# AI Operations Assistant Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it contains sensitive data!

# Azure AI Foundry Local Configuration
FOUNDRY_ENDPOINT=http://localhost:8000
FOUNDRY_MODEL=your-model-name
FOUNDRY_TIMEOUT=30

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Context Buffer Configuration (hours of cluster data to retain)
CONTEXT_BUFFER_HOURS=24

# Logging Configuration
LOG_LEVEL=INFO

# Kubernetes Configuration (if needed)
# KUBECONFIG=/path/to/kubeconfig
# KUBERNETES_NAMESPACE=default

# Optional: Azure credentials (if deploying to Azure)
# AZURE_SUBSCRIPTION_ID=your-subscription-id
# AZURE_RESOURCE_GROUP=your-resource-group
# AZURE_CLUSTER_NAME=your-cluster-name
